[package]
name = "scratchpad"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
eframe = { version = "0.27", features = ["persistence"] }
rfd = "0.14"
serde = { version = "1", features = ["derive"] }
regex = "1"
arboard = "3"

[build-dependencies]
ico = "0.3"
winres = "0.1"
